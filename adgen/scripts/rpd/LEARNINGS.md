# RPD experiment log

One row per meaningful run. Newest first. Add: date, spec, settled spend, gallery URL, takeaway.
(The gallery holds the full notes; this file is the index everyone can skim.)

Galleries live on **Netlify**, site `rs-rpd` in the **Flood QRF** team (Pro, so site
password protection is available — `secure_site`). Each row links a per-deploy URL,
which is immutable: it still shows THAT run later. `manifest.json` is deliberately
not published (it is the run ledger — prompts, prediction ids, costs).

| date | spec | spend (settled) | gallery | takeaway |
|---|---|---|---|---|
| 2026-09-08 | scene-preserve-onfigureplain-{studio,regression,jacket} + studio-vocab-{soludos-floor,pelagic-whitestudio,regression-soludos-interior,regression-pelagic-harbor,gymshark-leggings,gymshark-tshirt} | $1.445 (9 cells $0.694 + 4 cells $0.511 + 2 cells $0.240) | Claude artifact (no Netlify — published per the operator's own request instead of the usual gallery flow): https://claude.ai/code/artifact/d7df08fc-a411-4d7f-b615-20261e57ad64 | Four rounds, one continuous `/loop`. (1) `resolveSeedStyle`'s coarse lifestyle/on_model bucket was letting a person-on-blank-studio-backdrop seed get SCENE_PRESERVE'd (the reported bug: ads shipping with a blank background). Wired the pre-existing, unused `resolveSeedClass` finer classifier (`on_figure_plain` vs `lifestyle_scene`) into the veto — 9/9 live cells behaved as designed across 2 products. (2) Live-sampling 6 real catalog seeds across 3 brands found the classifier ITSELF had a gap: `sceneType` labels "White Studio"/"Studio Floor" weren't matching the studio-detector's anchored vocabulary. Fixed by extending the anchor's prefix/suffix word list (not loosening the match mechanism — a broader first attempt broke a deliberate `Beach`+contradictory-`setting` regression guard). 4/4 live cells confirmed, producing genuinely on-brand new scenes (marina/dock for fishing gear, garden for footwear). (3) A third, more different brand (Gymshark, athletic wear) confirmed the underlying mechanism generalizes — no code change, 2/2 live cells built genuine gym scenes (kettlebells, weight plates, natural light) rather than generic backdrops. (4) A full frequency survey of all 60 real `sceneType` labels in the DB closed two more confirmed gaps ("Ecommerce Studio", "Studio Detail" — each checked against its own real photo description before adding) while explicitly REJECTING two look-alike candidates ("Indoor Studio", "Minimal Studio" — same studio-sounding wording, but real photos show an actual room; "Minimal Studio" is notable because its reverse word order, "Studio Minimal", is already-accepted vocabulary, proving word order/wording alone isn't reliable evidence). All four rounds Grok-reviewed (grok-4.6, high effort, twice); `scripts/verifySeedClass.js` 265→280 checks. All fixes remain flag-gated OFF (`SEED_CLASS_SCENE_BASED`) pending the operator's own go-ahead to flip it in production. |
| 2026-08-18 | static-fidelity-block-ab | $0.2168 (4 cells: gpt-image-2/edit $0.072272 x2, -developer/edit $0.036136 + $0.04) | https://6a85134ead324b7a235aa4a8--rs-rpd.netlify.app | **Null result, recorded as such:** replacing the whole PRODUCT_FIDELITY block with a short product-specific version did NOT measurably beat the canonical block at n=1/arm — both arms preserved the printed logo lockup and its text and invented nothing. **Price points now measured** (the harness refuses to price unmeasured quality/size): standard $0.0723, developer $0.0361 — the developer variant really is ~half, and both matched the estimate table within $0.0005. Submit+poll 58-62s per plate. Auto-eval (production judge, gemini-2.5-pro) scored all four PASS 10/10 on product fidelity and text defects, corroborating the human frame read — so the null result is agreed by both. |
| 2026-08-18 | rpd-validation-crossfade-ab | $0.90 (2 × $0.45, Omni dev 4s 1080p) | https://6a851398423b22fe310390e8--rs-rpd.netlify.app | The transitions directive is live-effective: baseline shows mid-crossfade ghosting at ~1.2s/~2.5s; a one-line "hard cuts only" patch removed it in every sampled frame. Both arms hallucinated a neck-tag view absent from the seed (fidelity class, model-side). Titling chrome validated via the free resume pass (canonical preset, Stories safe zone); fixture proof-copy defaults removed the same day — proof fields render only when supplied. 4s settled price $0.45 vs $0.60 formula (~25% over, same direction as 10s). Cold Cloudinary transform 1044ms; queue→terminal 91–122s; Atlas publishes executionTime=0 on this model — use queueToTerminalMs. |
